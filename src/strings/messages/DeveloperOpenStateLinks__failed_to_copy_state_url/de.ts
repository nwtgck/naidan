export const DeveloperOpenStateLinks__failed_to_copy_state_url = ({ errorMessage }: { errorMessage: string }): string => (
  `URL des aktuellen Zustands konnte nicht kopiert werden: ${errorMessage}`
);
