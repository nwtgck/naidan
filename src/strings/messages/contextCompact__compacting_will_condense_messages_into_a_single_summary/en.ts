export const contextCompact__compacting_will_condense_messages_into_a_single_summary = ({ count }: { count: number }): string => (
  `Compacting will condense the first ${count} messages into a single summary. This reduces token usage while preserving the core context.`
);
