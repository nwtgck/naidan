export const ModelSupportInvestigationModal__model_file_plan_summary = ({ eligibleCount, candidateCount, registryFailureCount }: { eligibleCount: number; candidateCount: number; registryFailureCount: number }): string => `${eligibleCount} von ${candidateCount} ${candidateCount === 1 ? 'Kandidat hat' : 'Kandidaten haben'} alle erforderlichen Repository-Dateien · ${registryFailureCount} ${registryFailureCount === 1 ? 'Registry-Fehler' : 'Registry-Fehler'}`;
