export const StandaloneVerificationPage__verification_failed_to_run = ({ errorMessage }: { errorMessage: string }): string => (
  `Standalone-Verifizierung konnte nicht ausgeführt werden: ${errorMessage}`
);
