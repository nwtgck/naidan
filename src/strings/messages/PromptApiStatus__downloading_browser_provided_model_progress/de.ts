export const PromptApiStatus__downloading_browser_provided_model_progress = ({ progress }: { progress: number }): string => (
  `Vom Browser bereitgestelltes Modell wird heruntergeladen: ${progress}%`
);
