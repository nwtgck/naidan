export const volumes__delete_folder_warning = ({ name }: { name: string }): string => (
  `¿Seguro que quieres eliminar «${name}»? Esto eliminará permanentemente todos los datos copiados del navegador.`
);
