export const volumes__delete_folder_warning = ({ name }: { name: string }): string => (
  `Are you sure you want to delete "${name}"? This will permanently delete all copied data from the browser.`
);
