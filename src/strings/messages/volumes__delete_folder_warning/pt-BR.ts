export const volumes__delete_folder_warning = ({ name }: { name: string }): string => (
  `Tem certeza de que deseja excluir “${name}”? Isso excluirá permanentemente todos os dados copiados do navegador.`
);
