export const ModelSupportInvestigationModal__reasoning_differential_failed = ({ effort, name, message }: { effort: "none" | "high", name: string, message: string }): string => `Differenz der Production-Reasoning-Intensität bei ${effort} fehlgeschlagen: ${name}: ${message}`;
