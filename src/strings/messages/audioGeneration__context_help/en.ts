export const audioGeneration__context_help = (): string => "This is the requested context size; allocation is capped at the model's training context and may be reduced if memory is insufficient. A larger context uses more memory and does not make generation faster. It is separate from the audio step limit.";
