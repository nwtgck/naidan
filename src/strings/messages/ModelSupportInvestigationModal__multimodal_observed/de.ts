export const ModelSupportInvestigationModal__multimodal_observed = ({ strategy, fixtureId, width, height, mimeType, inputTensorCount, generatedTokenCount }: { strategy: string, fixtureId: string, width: number, height: number, mimeType: string, inputTensorCount: number, generatedTokenCount: number }): string => `Production-Multimodal-Test (${strategy}): festes ${fixtureId} ${width}x${height} ${mimeType}; ${inputTensorCount === 1 ? 'Eingabe-Tensor' : 'Eingabe-Tensoren'}=${inputTensorCount}; generiert=${generatedTokenCount} ${generatedTokenCount === 1 ? 'Token' : 'Tokens'}; Ausgabequalität wurde nicht bewertet`;
