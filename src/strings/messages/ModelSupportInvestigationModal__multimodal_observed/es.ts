export const ModelSupportInvestigationModal__multimodal_observed = ({ strategy, fixtureId, width, height, mimeType, inputTensorCount, generatedTokenCount }: { strategy: string, fixtureId: string, width: number, height: number, mimeType: string, inputTensorCount: number, generatedTokenCount: number }): string => `Prueba multimodal de Production (${strategy}): ${fixtureId} fijo ${width}x${height} ${mimeType}; ${inputTensorCount === 1 ? 'tensor de entrada' : 'tensores de entrada'}=${inputTensorCount}; generados=${generatedTokenCount} ${generatedTokenCount === 1 ? 'token' : 'tokens'}; no se evaluó la calidad de la salida`;
