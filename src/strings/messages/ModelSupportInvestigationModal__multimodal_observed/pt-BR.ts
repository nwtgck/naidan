export const ModelSupportInvestigationModal__multimodal_observed = ({ strategy, fixtureId, width, height, mimeType, inputTensorCount, generatedTokenCount }: { strategy: string, fixtureId: string, width: number, height: number, mimeType: string, inputTensorCount: number, generatedTokenCount: number }): string => `Teste multimodal de Production (${strategy}): ${fixtureId} fixo ${width}x${height} ${mimeType}; ${inputTensorCount === 1 ? 'tensor de entrada' : 'tensores de entrada'}=${inputTensorCount}; gerados=${generatedTokenCount} ${generatedTokenCount === 1 ? 'token' : 'tokens'}; a qualidade da saída não foi avaliada`;
