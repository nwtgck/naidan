export const ModelSupportInvestigationModal__multimodal_observed = ({ strategy, fixtureId, width, height, mimeType, inputTensorCount, generatedTokenCount }: { strategy: string, fixtureId: string, width: number, height: number, mimeType: string, inputTensorCount: number, generatedTokenCount: number }): string => `Production multimodal probe (${strategy}): fixed ${fixtureId} ${width}x${height} ${mimeType}; input tensors=${inputTensorCount}; generated=${generatedTokenCount} token(s); output quality was not evaluated`;
