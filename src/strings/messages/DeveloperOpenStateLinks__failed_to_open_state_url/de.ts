export const DeveloperOpenStateLinks__failed_to_open_state_url = ({ errorMessage }: { errorMessage: string }): string => (
  `URL des aktuellen Zustands konnte nicht geöffnet werden: ${errorMessage}`
);
