export const StandaloneVerificationPage__failed_to_copy_verification_json = ({ errorMessage }: { errorMessage: string }): string => (
  `Standalone-Verifizierungs-JSON konnte nicht kopiert werden: ${errorMessage}`
);
