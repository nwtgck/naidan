export const contextCompact__generating_compact_context_with_characters_received = ({ outputChars }: { outputChars: number }): string => (
  `Gerando contexto compactado, ${outputChars} ${outputChars === 1 ? 'caractere recebido' : 'caracteres recebidos'}.`
);
