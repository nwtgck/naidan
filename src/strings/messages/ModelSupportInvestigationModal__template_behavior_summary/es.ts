export const ModelSupportInvestigationModal__template_behavior_summary = ({ tokenizerClass, passedCount, failedCount }: { tokenizerClass: string; passedCount: number; failedCount: number }): string => `${tokenizerClass}: ${passedCount} ${passedCount === 1 ? 'caso renderizado' : 'casos renderizados'}, ${failedCount} ${failedCount === 1 ? 'caso no compatible o fallido' : 'casos no compatibles o fallidos'}`;
