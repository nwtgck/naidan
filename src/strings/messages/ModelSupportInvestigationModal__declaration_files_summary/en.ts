export const ModelSupportInvestigationModal__declaration_files_summary = ({ fileCount, revision }: { fileCount: number; revision: string }): string => `${fileCount} declaration files from ${revision}`;
