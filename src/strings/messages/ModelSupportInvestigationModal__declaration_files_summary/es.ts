export const ModelSupportInvestigationModal__declaration_files_summary = ({ fileCount, revision }: { fileCount: number; revision: string }): string => `${fileCount} ${fileCount === 1 ? 'archivo de declaración' : 'archivos de declaración'} de ${revision}`;
