export const stableDiffusionCppBrowser__gpu_budget_help = (): string => "Start with 2048 MiB (2 GiB). stable-diffusion.cpp uses this budget to stage weights and working buffers, not to limit model file size. Models can exceed it if each execution segment fits. Too small can fail or increase transfers; too large can exhaust memory. This is not a browser-wide memory cap or a reading of free GPU memory. Wasm32 accounting requires less than 4096 MiB.";
