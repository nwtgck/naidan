export const stableDiffusionCppBrowser__gpu_budget_help = (): string => "Leave empty to use device allocation limits without a managed budget. An optional budget covers resident weights and working buffers, not model file size. Too small can prevent generation or increase transfers; a larger value does not guarantee available memory. Explicit Wasm32 budgets must be below 4096 MiB.";
