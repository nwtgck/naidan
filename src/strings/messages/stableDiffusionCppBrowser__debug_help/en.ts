export const stableDiffusionCppBrowser__debug_help = (): string => "Enable beside Generate before starting to record per-run/step wall time, transfer bytes and existing GPU waits. These waits are not GPU execution timestamps. Repeated waits can overlap; use the union, not just their sum. Saved logs are bounded and stay local; prompts and token dumps are suppressed, but review filenames and technical details before sharing.";
