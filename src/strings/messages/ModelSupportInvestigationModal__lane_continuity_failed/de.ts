export const ModelSupportInvestigationModal__lane_continuity_failed = ({ name, message }: { name: string, message: string }): string => `Zweiter Production-Durchlauf fehlgeschlagen: ${name}: ${message}`;
