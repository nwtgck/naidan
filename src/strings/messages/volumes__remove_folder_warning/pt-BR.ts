export const volumes__remove_folder_warning = ({ name }: { name: string }): string => (
  `Tem certeza de que deseja remover “${name}”? Isso interromperá o uso da pasta. Seus arquivos originais não serão afetados.`
);
