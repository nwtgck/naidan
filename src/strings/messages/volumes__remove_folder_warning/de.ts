export const volumes__remove_folder_warning = ({ name }: { name: string }): string => (
  `„${name}“ wirklich entfernen? Der Ordner wird nicht mehr verwendet. Deine Originaldateien bleiben unverändert.`
);
