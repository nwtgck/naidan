export const ModelSupportInvestigationModal__lane_continuity_summary = ({ cacheProvided, mode, exactPrefixMatch, firstMismatchIndex, secondInputTokenCount }: { cacheProvided: boolean, mode: string, exactPrefixMatch: boolean | undefined, firstMismatchIndex: number | undefined, secondInputTokenCount: number }): string => `Zweiter Turn: ${cacheProvided ? "KV-Cache bereitgestellt" : "KV-Cache nicht bereitgestellt"}; ${mode}; Eingabe=${secondInputTokenCount} ${secondInputTokenCount === 1 ? "Token" : "Tokens"}; exakter Präfix=${exactPrefixMatch === undefined ? "nicht abgeleitet" : exactPrefixMatch ? "ja" : `nein bei ${firstMismatchIndex ?? "Grenze"}`}`;
