export const ModelSupportInvestigationModal__lane_continuity_summary = ({ cacheProvided, cacheDecisionStatus, cacheDecisionReason, mode, comparisonInputSource, exactPrefixMatch, firstMismatchIndex, secondInputTokenCount, mismatchExpectedText, mismatchActualText }: { cacheProvided: boolean, cacheDecisionStatus: string, cacheDecisionReason: string, mode: string, comparisonInputSource: string, exactPrefixMatch: boolean | undefined, firstMismatchIndex: number | undefined, secondInputTokenCount: number, mismatchExpectedText: string | undefined, mismatchActualText: string | undefined }): string => `第二轮：${cacheProvided ? "已提供 KV 缓存" : "未提供 KV 缓存"}；缓存决策=${cacheDecisionStatus} (${cacheDecisionReason})；${mode}；比较输入=${comparisonInputSource}；输入=${secondInputTokenCount} 个 token；精确前缀=${exactPrefixMatch === undefined ? "未推断" : exactPrefixMatch ? "是" : `否，位置 ${firstMismatchIndex ?? "边界"}`}${mismatchExpectedText === undefined && mismatchActualText === undefined ? "" : `；不匹配预期=${mismatchExpectedText ?? "不可用"}；实际=${mismatchActualText ?? "不可用"}`}`;
