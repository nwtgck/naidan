export const ModelSupportInvestigationModal__lane_continuity_summary = ({ cacheProvided, mode, exactPrefixMatch, firstMismatchIndex, secondInputTokenCount }: { cacheProvided: boolean, mode: string, exactPrefixMatch: boolean | undefined, firstMismatchIndex: number | undefined, secondInputTokenCount: number }): string => `Segunda rodada: ${cacheProvided ? "cache KV fornecido" : "cache KV não fornecido"}; ${mode}; entrada=${secondInputTokenCount} ${secondInputTokenCount === 1 ? "token" : "tokens"}; prefixo exato=${exactPrefixMatch === undefined ? "não inferido" : exactPrefixMatch ? "sim" : `não em ${firstMismatchIndex ?? "limite"}`}`;
