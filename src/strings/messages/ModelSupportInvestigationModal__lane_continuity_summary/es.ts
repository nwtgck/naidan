export const ModelSupportInvestigationModal__lane_continuity_summary = ({ cacheProvided, mode, exactPrefixMatch, firstMismatchIndex, secondInputTokenCount }: { cacheProvided: boolean, mode: string, exactPrefixMatch: boolean | undefined, firstMismatchIndex: number | undefined, secondInputTokenCount: number }): string => `Segundo turno: ${cacheProvided ? "caché KV proporcionado" : "caché KV no proporcionado"}; ${mode}; entrada=${secondInputTokenCount} ${secondInputTokenCount === 1 ? "token" : "tokens"}; prefijo exacto=${exactPrefixMatch === undefined ? "no inferido" : exactPrefixMatch ? "sí" : `no en ${firstMismatchIndex ?? "límite"}`}`;
