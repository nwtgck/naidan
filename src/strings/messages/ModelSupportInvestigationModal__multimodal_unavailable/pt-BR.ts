export const ModelSupportInvestigationModal__multimodal_unavailable = ({ strategy, reason }: { strategy: string, reason: string }): string => `Teste multimodal de Production indisponível (${strategy}): ${reason}`;
