export const ModelSupportInvestigationModal__support_boundary_summary = ({ count, boundaries }: { count: number; boundaries: string }): string => `${count} ${count === 1 ? 'evaluación' : 'evaluaciones'} · ${boundaries}`;
