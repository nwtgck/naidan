export const StorageTab__confirm_switch_to_storage = ({ storageName }: { storageName: string }): string => (
  `Möchtest du wirklich zu ${storageName} wechseln? Dabei werden alle Daten migriert und die Anwendung wird neu geladen.`
);
