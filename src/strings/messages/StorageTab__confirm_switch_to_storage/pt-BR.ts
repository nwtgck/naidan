export const StorageTab__confirm_switch_to_storage = ({ storageName }: { storageName: string }): string => (
  `Tem certeza de que deseja mudar para ${storageName}? Todos os dados serão migrados e o aplicativo será recarregado.`
);
