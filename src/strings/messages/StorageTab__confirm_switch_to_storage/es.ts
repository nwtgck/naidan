export const StorageTab__confirm_switch_to_storage = ({ storageName }: { storageName: string }): string => (
  `¿Seguro que quieres cambiar a ${storageName}? Se migrarán todos los datos y la aplicación se recargará.`
);
