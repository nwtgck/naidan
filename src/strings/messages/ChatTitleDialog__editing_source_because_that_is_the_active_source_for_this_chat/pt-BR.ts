export const ChatTitleDialog__editing_source_because_that_is_the_active_source_for_this_chat = ({ sourceLabel }: { sourceLabel: string }): string => (
  `Editando ${sourceLabel} porque essa é a fonte ativa para esta conversa.`
);
